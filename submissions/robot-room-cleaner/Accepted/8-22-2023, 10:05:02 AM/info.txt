{"id":1028170587,"question_id":865,"lang":"java","lang_name":"Java","time":"9 months, 3 weeks","timestamp":1692669902,"status":10,"status_display":"Accepted","runtime":"13 ms","url":"/submissions/detail/1028170587/","is_pending":"Not Pending","title":"Robot Room Cleaner","memory":"43.6 MB","code":"/**\n * // This is the robot's control interface.\n * // You should not implement it, or speculate about its implementation\n * interface Robot {\n *     // Returns true if the cell in front is open and robot moves into the cell.\n *     // Returns false if the cell in front is blocked and robot stays in the current cell.\n *     public boolean move();\n *\n *     // Robot will stay in the same cell after calling turnLeft/turnRight.\n *     // Each turn will be 90 degrees.\n *     public void turnLeft();\n *     public void turnRight();\n *\n *     // Clean the current cell.\n *     public void clean();\n * }\n */\n\nclass Solution {\n    \n    int[][] dirs = {{0, 1}, {-1, 0}, {0, -1}, {1, 0}}; // must be clockwise  (or anti-clockwise is turnLeft)\n    public void cleanRoom(Robot robot) {\n        Set<String> visited = new HashSet<>();\n        dfs(robot, 0, 0, 0, visited);\n    }\n    \n    // [x, y] is the relative position from the initial point\n    private void dfs(Robot robot, int x, int y, int curDir, Set<String> visited) {\n        visited.add(x + \"-\" + y);\n        \n        robot.clean();\n        for (int i = 0; i < 4; i++) {\n            int nextDir = (curDir + i) % 4;  // i==0, remain same dir\n            int newX = x + dirs[nextDir][0]; // get the next x, y\n            int newY = y + dirs[nextDir][1];\n            \n            if (!visited.contains(newX + \"-\" + newY) && robot.move()) {\n                dfs(robot, newX, newY, nextDir, visited);\n            }\n            \n            robot.turnRight();\n        }\n        \n        \n        // go back one block\n        robot.turnRight();\n        robot.turnRight();\n        robot.move();\n        robot.turnRight();\n        robot.turnRight();\n    }\n}","compare_result":"11111111111111111111111111111","title_slug":"robot-room-cleaner","has_notes":false,"flag_type":1}